{% extends 'base.html.twig' %}

{% block title %}UserBox{% endblock %}

{% block body %}
    <h1>UserBox</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_box.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ user_box.createdAt ? user_box.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_box_index') }}">back to list</a>

    <a href="{{ path('user_box_edit', {'id': user_box.id}) }}">edit</a>

    {{ include('user_box/_delete_form.html.twig') }}
{% endblock %}
