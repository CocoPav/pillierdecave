{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>
<div class="container_table">
    <table class="table">
        <tbody>
            <tr>
                <th>Email</th>
                <td>{{ app.user.email }}</td>
            </tr>

            <tr>
                <th>Firstname</th>
                <td>{{ app.user.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ app.user.lastname }}</td>
            </tr>
            <tr>
                <th>Birthdate</th>
                <td>{{ app.user.birthdate ? app.user.birthdate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div>
        <h3> Mes Abonnements </h3>
    {% for userBox in app.user.userBoxes %}
        <tr>

            <th>Box {{ userBox.box.title }}</th>
            <td>{{ userBox.box.price }}€</td>

        </tr>
    {% endfor %}
    </div>

</div>

    {% include 'user/_delete_form.html.twig' with {'user': app.user} only %}
{% endblock %}
